[package]
name = "universal-util"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
slint = "1.6"
image = "0.24"
rfd = "0.14"
rayon = "1.8"
oxipng = "9.1"
infer = "0.19"
anyhow = "1.0"
rust_ffmpeg = "0.1"
futures = "0.3"


[build-dependencies]
slint-build = "1.6" # Compiles your .slint UI files to Rust code
cc = "1.0"          # Allows Rust to compile your C++ files during 'cargo build'
